/// Sideways-cheat detection uses a simple portrait/landscape lock.
/// Sideways mode expects portrait (so rotating to landscape = "easier tapping" cheat).
enum DeviceLock { none, portraitOnly, landscapeOnly }
